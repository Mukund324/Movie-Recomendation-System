### Setup
- Create the virtual env
  ```sh
  $ python -m venv venv
  ```

- Install the dependencies
  ```sh
  $ pip install -r requirements.txt
  ```

- Run the flask app
  ```sh
  $ export FLASK_APP=myapp
  $ flask run
  ```